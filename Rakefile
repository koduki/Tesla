# package dfines
APP_NAME = "tesla"
TARGET = "gen_testcase.rb"
FILES = [
  "gen_testcase.rb",
  "lib/*",
]

# work directories
WORK_DIR = "build"
DIST_DIR = "dist"

task :default => "jar:package"
load "./rake/packagejar"
